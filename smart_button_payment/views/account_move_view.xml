<odoo>
    <data>
        <record id="view_move_form_payment_button" model="ir.ui.view">
            <field name="name">account.move.form.payment.smart.button</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">

                <xpath expr="//div[@name='button_box']" position="inside">
                    <button name="action_view_payments"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-money"
                            invisible="payment_count == 0 or
                               move_type not in
                               ('out_invoice','in_invoice','out_refund','in_refund','out_receipt','in_receipt')">
                        <field name="payment_count" widget="statinfo" string="Payments"/>
                    </button>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
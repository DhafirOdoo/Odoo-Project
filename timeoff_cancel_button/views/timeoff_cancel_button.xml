<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <record id="hr_leave_form_inherit_cancel_button" model="ir.ui.view">
            <field name="name">hr.leave.form.inherit.cancel.button</field>
            <field name="model">hr.leave</field>
            <field name="inherit_id" ref="hr_holidays.hr_leave_view_form"/>
            <field name="arch" type="xml">

                <xpath expr="//div[contains(@class, 'col_left')]" position="after">
                    <div class="o_hr_leave_column col_right col-md-6 col-12" invisible="cancel_requested == False">
                        <group string="Cancel Info">
                            <field name="cancel_requested" invisible="1"/>
                            <field name="is_current_user" invisible="1"/>
                            <field name="cancel_reason" invisible="cancel_requested == False"
                                   readonly="cancel_requested"/>
                        </group>
                    </div>
                </xpath>
                <xpath expr="//button[@name='action_cancel']" position="attributes">
                    <attribute name="invisible">not active or not can_cancel or cancel_requested or not is_current_user</attribute>
                </xpath>
                <xpath expr="//field[@name='name']" position="attributes">
                    <attribute name="required">1</attribute>
                </xpath>
                <xpath expr="//button[@name='action_validate']" position="attributes">
                    <attribute name="invisible">state != 'validate1' or cancel_requested or not active</attribute>
                </xpath>
                <xpath expr="//button[@name='action_refuse']" position="attributes">
                     <attribute name="invisible">not active or not can_approve or state not in ('confirm', 'validate1', 'validate') or cancel_requested</attribute>
                </xpath>
            </field>
        </record>
        <record id="hr_leave_form_manager_inherit_cancel_button" model="ir.ui.view">
            <field name="name">hr.leave.form.manager.inherit.cancel.button</field>
            <field name="model">hr.leave</field>
            <field name="inherit_id" ref="hr_holidays.hr_leave_view_form_manager"/>
            <field name="arch" type="xml">
                <xpath expr="//header" position="inside">
                    <button string="Cancel Leave" name="manager_cancel" type="object" invisible="not cancel_requested"
                    groups="timeoff_cancel_button.group_timeoff_cancel_user"/>
                </xpath>
            </field>
        </record>

        <record id="action_timeoff_cancel_request" model="ir.actions.act_window">
            <field name="name">Time Off Cancel Requests</field>
            <field name="res_model">hr.leave</field>
            <field name="view_mode">tree,form</field>
            <field name="domain">[('cancel_requested', '=', True)]</field>
        </record>
    </data>
</odoo>